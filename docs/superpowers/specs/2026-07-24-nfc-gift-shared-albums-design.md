# NFC 礼品共享相册设计

每件礼品使用预登记的高熵 HTTPS token。首次通过验证码的用户认领礼品并绑定一册本机旅行册或新建旅行册；管理者和受邀人合计最多三人。管理者触碰后进入管理页，受邀人触碰后进入只读共享相册，其他人只收到无权限提示。

共享内容采用手动发布快照。服务端为私有 R2 对象签发短期上传和读取 URL，发布提交后原子替换当前相册版本。停用礼品会删除云端快照及媒体并永久禁用 token。
