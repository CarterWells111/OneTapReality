# NFC 开发者管理台设计

空卡先写入统一 `/activate` HTTPS URL。仅白名单开发者可在原生 App 中创建 15 分钟初始化预留、写入并验证唯一礼品 URL，随后将实体卡激活。实体卡不依赖或采集 NFC UID；其可视化库存身份是服务端短卡号和可选备注。

`gift_cards` 保存实体卡生命周期、关联礼品和操作人；`gift_card_events` 保存初始化、激活、过期和作废审计。普通用户继续只使用唯一 `/gift/<token>`，不接触开发者管理接口。写卡由窄原生适配器承担，Expo Go 只能显示不支持提示。
