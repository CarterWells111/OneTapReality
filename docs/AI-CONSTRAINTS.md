# AI 约束

- 本版唯一实现是 `DemoDraftGenerator`。
- 它仅使用用户填写的标题、城市、日期、照片数量和本地照片顺序生成固定旅行册结构。
- 它不读取图像像素、不识别人物、地点或内容，也不声称具备上述能力。
- 它不使用 `fetch`、模型 SDK、环境变量、远程 URL 或任何上传行为。
- 用户可在保存前编辑全部生成文本。
- 将来接入真实模型时，必须使用服务端代理、显式同意和最小化数据；客户端不得持有模型密钥。

