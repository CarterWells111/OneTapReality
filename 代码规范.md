# OneTapReality 代码规范

> 适用仓库：<https://github.com/CarterWells111/OneTapReality>
>
> 本文只规定代码实现与提交质量。产品范围、功能优先级、演示流程、硬件方案等不在本文范围内。

## 1. 规则优先级

发生冲突时，按以下顺序处理：

1. 保证现有功能可运行；
2. 保持 Expo Go 与当前 Expo SDK 兼容；
3. 通过 TypeScript、Lint 和 Expo Doctor 检查；
4. 再进行重构或视觉优化。

不得为了局部整洁重写现有架构。修改前先搜索仓库中是否已有同类组件、Hook、类型、常量或工具函数。

## 2. 修改范围

- 优先修改现有文件；只有在职责无法合理归入现有文件时才新建文件。
- 一个提交只处理一个明确问题，不混入无关格式化、重命名或依赖升级。
- 不删除看似未使用但用途不明确的代码；先检查路由、动态导入、配置文件和字符串引用。
- 不改动锁文件，除非本次任务确实安装、删除或升级了依赖。
- 不手工编辑自动生成文件。
- 未经明确要求，不新增状态管理框架、UI 框架或原生依赖。

## 3. TypeScript

### 3.1 必须启用严格类型检查

`tsconfig.json` 应继承 Expo 默认配置并启用 `strict`：

```json
{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  }
}
```

若仓库已有额外配置，应保留原配置，只补充缺失项，不直接覆盖整个文件。

### 3.2 禁止 `any`

- 外部输入、API 响应、反序列化结果统一使用 `unknown`。
- 必须先验证 `unknown`，再转换为业务类型。
- 不用 `as SomeType` 跳过运行时校验。
- 不用 `// @ts-ignore`。确需压制单行错误时只能使用 `// @ts-expect-error`，并写明原因。
- 避免非空断言 `!`；先处理 `null` 或 `undefined`。

可直接复用以下基础工具：

```typescript
// utils/type-guards.ts
export type UnknownRecord = Record<string, unknown>;

export function isRecord(value: unknown): value is UnknownRecord {
  return typeof value === 'object' && value !== null && !Array.isArray(value);
}

export function getErrorMessage(error: unknown): string {
  if (error instanceof Error) {
    return error.message;
  }

  if (typeof error === 'string') {
    return error;
  }

  return 'Unknown error';
}

export function assertNever(value: never): never {
  throw new Error(`Unhandled value: ${String(value)}`);
}
```

### 3.3 类型定义

- 对象数据结构使用 `interface`。
- 联合类型、字面量类型和工具组合使用 `type`。
- 组件 Props 使用 `Readonly<...>`。
- 跨文件共享的类型必须 `export`；只在单文件使用的类型不得导出。
- 状态字段使用字面量联合，不使用无约束的 `string`。

```typescript
export type GenerationStage =
  | 'idle'
  | 'preprocessing'
  | 'uploading'
  | 'generating'
  | 'rendering'
  | 'exporting'
  | 'completed'
  | 'failed';

export interface GenerationState {
  stage: GenerationStage;
  progress: number;
  error: string | null;
}
```

## 4. 命名与文件

| 对象 | 规则 | 示例 |
|---|---|---|
| React 组件 | PascalCase | `AlbumCard` |
| Hook | `use` + camelCase | `useAlbum` |
| 普通函数和变量 | camelCase | `generateAlbum` |
| 类型和接口 | PascalCase | `AlbumDocument` |
| 常量 | SCREAMING_SNAKE_CASE | `MAX_PHOTO_COUNT` |
| 组件文件 | kebab-case `.tsx` | `album-card.tsx` |
| 非组件文件 | kebab-case `.ts` | `album-validator.ts` |
| 路由文件 | 遵循 Expo Router 文件路由规则 | `[albumId].tsx` |

其他规则：

- 布尔值使用 `is`、`has`、`can` 或 `should` 前缀。
- 事件处理函数使用 `handle` 前缀；传入子组件的回调 Props 使用 `on` 前缀。
- 不使用 `data`、`item`、`temp`、`value` 等缺少语义的长期变量名。
- 文件名与默认导出的页面或组件职责一致。

## 5. 导入规范

导入按以下顺序排列，并用空行分组：

1. React；
2. React Native 与 Expo；
3. 第三方依赖；
4. `@/` 绝对路径；
5. 相对路径；
6. `import type` 类型导入。

要求：

- 能用 `@/` 时不要编写多层 `../../../`。
- 仅用于类型的导入必须使用 `import type`。
- 不创建只做无意义转发的 barrel 文件；已有统一导出文件可以继续使用。
- 不通过循环依赖解决共享类型；把共享类型移动到更低层的类型文件。

## 6. React 与 React Native

### 6.1 组件

- 只使用函数组件。
- 页面组件只负责编排数据和页面结构；可复用 UI 下沉到组件。
- 单个组件只承担一个主要职责。
- 不在渲染期间发起请求、写存储或修改状态。
- 列表必须使用稳定且唯一的 `key`，不得默认使用数组下标。
- 样式使用 `StyleSheet.create`；仅动态值保留为内联样式。
- 颜色、间距、圆角等重复视觉值使用现有主题或常量。

可直接使用的按钮组件示例：

```tsx
// components/ui/primary-button.tsx
import { Pressable, StyleSheet, Text } from 'react-native';

interface PrimaryButtonProps {
  title: string;
  disabled?: boolean;
  onPress: () => void;
}

export function PrimaryButton({
  title,
  disabled = false,
  onPress,
}: Readonly<PrimaryButtonProps>) {
  return (
    <Pressable
      accessibilityRole="button"
      accessibilityState={{ disabled }}
      disabled={disabled}
      onPress={onPress}
      style={({ pressed }) => [
        styles.button,
        disabled && styles.disabled,
        pressed && !disabled && styles.pressed,
      ]}
    >
      <Text style={styles.label}>{title}</Text>
    </Pressable>
  );
}

const styles = StyleSheet.create({
  button: {
    alignItems: 'center',
    borderRadius: 12,
    justifyContent: 'center',
    minHeight: 48,
    paddingHorizontal: 20,
  },
  disabled: {
    opacity: 0.5,
  },
  label: {
    fontSize: 16,
    fontWeight: '600',
  },
  pressed: {
    opacity: 0.8,
  },
});
```

### 6.2 Hooks

- Hook 只能在组件或自定义 Hook 顶层调用。
- `useEffect` 只用于与 React 外部系统同步，不用于计算可由 Props 或 State 得出的值。
- `useMemo` 和 `useCallback` 只在有明确性能或引用稳定性需求时使用。
- Effect 中的异步任务必须处理组件卸载或请求取消。
- 依赖数组必须完整，不通过关闭 ESLint 规则隐藏问题。

安全的异步 Effect 示例：

```typescript
// hooks/use-album-summary.ts
import { useEffect, useState } from 'react';

import { API_BASE_URL } from '@/constants/env';
import { fetchJson } from '@/services/http/fetch-json';
import { getErrorMessage, isRecord } from '@/utils/type-guards';

interface AlbumSummary {
  id: string;
  title: string;
}

export function useAlbumSummary(albumId: string) {
  const [summary, setSummary] = useState<AlbumSummary | null>(null);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    const controller = new AbortController();

    async function loadSummary(): Promise<void> {
      try {
        const body = await fetchJson(
          `${API_BASE_URL}/api/albums/${encodeURIComponent(albumId)}`,
          { signal: controller.signal },
        );

        setSummary(parseAlbumSummary(body));
        setError(null);
      } catch (caughtError) {
        if (controller.signal.aborted) {
          return;
        }

        setError(getErrorMessage(caughtError));
      }
    }

    void loadSummary();

    return () => {
      controller.abort();
    };
  }, [albumId]);

  return { summary, error } as const;
}

function parseAlbumSummary(value: unknown): AlbumSummary {
  if (!isRecord(value)) {
    throw new Error('Album summary must be an object');
  }

  if (typeof value.id !== 'string' || typeof value.title !== 'string') {
    throw new Error('Album summary is missing required fields');
  }

  return {
    id: value.id,
    title: value.title,
  };
}
```

## 7. 状态管理

- 局部 UI 状态使用 `useState`。
- 具有多个关联字段或明确事件流的状态使用 `useReducer`。
- 跨多个页面共享且生命周期属于一次相册创建流程的数据，优先复用仓库已有 Context 或 Store。
- 不为一个布尔值引入全局状态。
- Reducer Action 使用可辨识联合，并进行穷尽检查。

```typescript
import { assertNever } from '@/utils/type-guards';

interface GenerationState {
  stage: 'idle' | 'running' | 'completed' | 'failed';
  progress: number;
  error: string | null;
}

type GenerationAction =
  | { type: 'START' }
  | { type: 'SET_PROGRESS'; progress: number }
  | { type: 'COMPLETE' }
  | { type: 'FAIL'; message: string }
  | { type: 'RESET' };

export const INITIAL_GENERATION_STATE: GenerationState = {
  stage: 'idle',
  progress: 0,
  error: null,
};

export function generationReducer(
  state: GenerationState,
  action: GenerationAction,
): GenerationState {
  switch (action.type) {
    case 'START':
      return { stage: 'running', progress: 0, error: null };
    case 'SET_PROGRESS':
      return {
        ...state,
        progress: Math.min(100, Math.max(0, action.progress)),
      };
    case 'COMPLETE':
      return { stage: 'completed', progress: 100, error: null };
    case 'FAIL':
      return { ...state, stage: 'failed', error: action.message };
    case 'RESET':
      return INITIAL_GENERATION_STATE;
    default:
      return assertNever(action);
  }
}
```

## 8. 异步操作与错误处理

### 8.1 基本要求

- 所有 Promise 必须 `await`、`return` 或显式使用 `void`。
- 用户触发的异步操作必须防止重复提交。
- 网络请求必须设置超时，并在页面离开时尽量取消。
- 对用户显示可理解的信息；技术细节写入日志，不直接展示堆栈。
- 单张照片失败时可局部降级；关键文档或导出失败时必须终止当前步骤并给出恢复路径。
- 不使用空 `catch`。

### 8.2 统一错误类型

```typescript
// utils/app-error.ts
export class AppError extends Error {
  readonly code: string;
  readonly details: unknown;

  constructor(code: string, message: string, details: unknown = null) {
    super(message);
    this.name = 'AppError';
    this.code = code;
    this.details = details;
  }
}
```

### 8.3 带超时的 JSON 请求

```typescript
// services/http/fetch-json.ts
import { AppError } from '@/utils/app-error';
import { getErrorMessage } from '@/utils/type-guards';

interface FetchJsonOptions extends RequestInit {
  timeoutMs?: number;
}

export async function fetchJson(
  url: string,
  options: FetchJsonOptions = {},
): Promise<unknown> {
  const {
    timeoutMs = 60_000,
    signal: externalSignal,
    ...requestInit
  } = options;
  const controller = new AbortController();
  let didTimeout = false;

  const handleExternalAbort = (): void => {
    controller.abort();
  };

  if (externalSignal?.aborted) {
    controller.abort();
  } else {
    externalSignal?.addEventListener('abort', handleExternalAbort, { once: true });
  }

  const timeoutId = setTimeout(() => {
    didTimeout = true;
    controller.abort();
  }, timeoutMs);

  try {
    const response = await fetch(url, {
      ...requestInit,
      signal: controller.signal,
    });

    const text = await response.text();
    let body: unknown = null;

    if (text.length > 0) {
      try {
        body = JSON.parse(text) as unknown;
      } catch (error) {
        throw new AppError('INVALID_JSON', 'Server returned invalid JSON', error);
      }
    }

    if (!response.ok) {
      throw new AppError(
        'HTTP_ERROR',
        `Request failed with status ${response.status}`,
        body,
      );
    }

    return body;
  } catch (error) {
    if (error instanceof AppError) {
      throw error;
    }

    if (error instanceof Error && error.name === 'AbortError') {
      if (didTimeout) {
        throw new AppError(
          'REQUEST_TIMEOUT',
          `Request timed out after ${timeoutMs} ms`,
        );
      }

      throw new AppError('REQUEST_ABORTED', 'Request was cancelled');
    }

    throw new AppError('NETWORK_ERROR', getErrorMessage(error), error);
  } finally {
    clearTimeout(timeoutId);
    externalSignal?.removeEventListener('abort', handleExternalAbort);
  }
}
```

调用方必须把返回的 `unknown` 立即交给对应的解析函数；不得在业务代码中直接读取未经校验的字段。

## 9. API 边界与运行时校验

任何来自以下位置的数据都不可信：

- 后端 API；
- AI 模型输出；
- AsyncStorage 或文件；
- URL 参数；
- 用户导入的 JSON。

校验函数必须返回一个全新的、满足业务类型的数据对象，不直接修改原始输入。

可直接使用的 AI 分析结果解析示例：

```typescript
// services/ai/parse-analysis-result.ts
import { AppError } from '@/utils/app-error';
import { isRecord } from '@/utils/type-guards';

export interface CropRect {
  originX: number;
  originY: number;
  width: number;
  height: number;
}

export interface SelectedPhotoAnalysis {
  photoId: string;
  score: number;
  description: string;
  cropRect: CropRect;
}

export interface AIAnalysisResult {
  selectedPhotos: SelectedPhotoAnalysis[];
}

export function parseAIAnalysisResult(value: unknown): AIAnalysisResult {
  if (!isRecord(value) || !Array.isArray(value.selectedPhotos)) {
    throw new AppError('INVALID_AI_RESPONSE', 'selectedPhotos must be an array');
  }

  return {
    selectedPhotos: value.selectedPhotos.map((item, index) =>
      parseSelectedPhoto(item, index),
    ),
  };
}

function parseSelectedPhoto(value: unknown, index: number): SelectedPhotoAnalysis {
  if (!isRecord(value)) {
    throw new AppError(
      'INVALID_AI_RESPONSE',
      `selectedPhotos[${index}] must be an object`,
    );
  }

  const { photoId, score, description, cropRect } = value;

  if (typeof photoId !== 'string' || photoId.length === 0) {
    throw new AppError(
      'INVALID_AI_RESPONSE',
      `selectedPhotos[${index}].photoId must be a non-empty string`,
    );
  }

  if (typeof score !== 'number' || !Number.isFinite(score)) {
    throw new AppError(
      'INVALID_AI_RESPONSE',
      `selectedPhotos[${index}].score must be a finite number`,
    );
  }

  if (typeof description !== 'string') {
    throw new AppError(
      'INVALID_AI_RESPONSE',
      `selectedPhotos[${index}].description must be a string`,
    );
  }

  return {
    photoId,
    score,
    description,
    cropRect: parseCropRect(cropRect, index),
  };
}

function parseCropRect(value: unknown, index: number): CropRect {
  if (!isRecord(value)) {
    throw new AppError(
      'INVALID_AI_RESPONSE',
      `selectedPhotos[${index}].cropRect must be an object`,
    );
  }

  const originX = readFiniteNumber(value.originX, `selectedPhotos[${index}].cropRect.originX`);
  const originY = readFiniteNumber(value.originY, `selectedPhotos[${index}].cropRect.originY`);
  const width = readFiniteNumber(value.width, `selectedPhotos[${index}].cropRect.width`);
  const height = readFiniteNumber(value.height, `selectedPhotos[${index}].cropRect.height`);

  if (originX < 0 || originY < 0 || width <= 0 || height <= 0) {
    throw new AppError(
      'INVALID_AI_RESPONSE',
      `selectedPhotos[${index}].cropRect contains invalid dimensions`,
    );
  }

  return { originX, originY, width, height };
}

function readFiniteNumber(value: unknown, path: string): number {
  if (typeof value !== 'number' || !Number.isFinite(value)) {
    throw new AppError('INVALID_AI_RESPONSE', `${path} must be a finite number`);
  }

  return value;
}
```

额外要求：

- 限制字符串长度、数组长度和图片数量。
- 验证所有照片 ID 引用确实存在。
- 验证裁切矩形没有越过原图边界；越界时拒绝或钳制，不能直接交给图片处理库。
- 不信任 AI 返回的文件名、URL、MIME 类型或模板名。

## 10. 并发与批处理

图片读取、压缩、上传和 Base64 转换不得对全部照片直接 `Promise.all`。使用明确的并发上限。

可直接使用以下工具：

```typescript
// utils/map-with-concurrency.ts
export async function mapWithConcurrency<T, R>(
  items: readonly T[],
  concurrency: number,
  mapper: (item: T, index: number) => Promise<R>,
): Promise<R[]> {
  if (!Number.isInteger(concurrency) || concurrency < 1) {
    throw new Error('concurrency must be a positive integer');
  }

  if (items.length === 0) {
    return [];
  }

  const results = new Array<R>(items.length);
  const iterator = items.entries();

  async function worker(): Promise<void> {
    for (;;) {
      const next = iterator.next();

      if (next.done) {
        return;
      }

      const [currentIndex, item] = next.value;
      results[currentIndex] = await mapper(item, currentIndex);
    }
  }

  const workerCount = Math.min(concurrency, items.length);
  await Promise.all(Array.from({ length: workerCount }, () => worker()));

  return results;
}
```

单项失败但允许继续时，调用方显式返回结果联合：

```typescript
import { getErrorMessage } from '@/utils/type-guards';
import { mapWithConcurrency } from '@/utils/map-with-concurrency';

interface Photo {
  id: string;
  uri: string;
}

interface ProcessedPhoto extends Photo {
  processedUri: string;
}

type PhotoProcessResult =
  | { ok: true; photo: ProcessedPhoto }
  | { ok: false; photoId: string; message: string };

export async function processPhotos(
  photos: readonly Photo[],
  processSinglePhoto: (uri: string) => Promise<string>,
): Promise<PhotoProcessResult[]> {
  return mapWithConcurrency<Photo, PhotoProcessResult>(
    photos,
    3,
    async (photo) => {
      try {
        const processedUri = await processSinglePhoto(photo.uri);
        return { ok: true, photo: { ...photo, processedUri } };
      } catch (error) {
        return {
          ok: false,
          photoId: photo.id,
          message: getErrorMessage(error),
        };
      }
    },
  );
}
```

## 11. Expo 依赖与兼容性

- 安装 Expo SDK 模块时使用 `npx expo install <package>`，不要直接猜测版本。
- 安装后运行 `npx expo install --check` 和 `npx expo-doctor`。
- 引入新依赖前确认其支持当前 Expo SDK、Expo Go 和 React Native 平台。
- 需要自定义原生代码、Config Plugin 或 Development Build 的依赖，不得在未评估迁移成本前加入。
- 不同时混用多个完成同一职责的库。
- 不为解决 TypeScript 报错手工锁定不兼容的依赖版本。

## 12. 环境变量与密钥

客户端只允许读取非敏感配置：

```typescript
// constants/env.ts
function requirePublicEnv(value: string | undefined, name: string): string {
  if (!value) {
    throw new Error(`Missing required environment variable: ${name}`);
  }

  return value;
}

export const API_BASE_URL = requirePublicEnv(
  process.env.EXPO_PUBLIC_API_BASE_URL,
  'EXPO_PUBLIC_API_BASE_URL',
);
```

要求：

- `EXPO_PUBLIC_*` 会进入客户端包，只能存放公开配置，不能存放 API Key、访问令牌或私钥。
- 敏感密钥只存在后端或安全的服务端环境变量中。
- 客户端读取 Expo 公共环境变量时使用静态点访问，例如 `process.env.EXPO_PUBLIC_API_BASE_URL`；不使用动态索引。
- `.env*` 是否提交由仓库现有策略决定；包含本地或敏感值的文件必须加入 `.gitignore`。
- 日志不得输出密钥、完整令牌、用户照片 Base64 或完整后端响应。

## 13. 注释与日志

### 注释

- 注释解释"为什么"，不重复代码"做了什么"。
- 临时方案必须标明原因和删除条件。
- `TODO` 必须包含明确任务，最好附 Issue 编号。
- 不保留大段注释掉的旧代码；版本历史由 Git 保存。

```typescript
// iOS 打印不读取本地 file:// 图片，因此在生成 HTML 前转换为 data URI。
const printableUri = await convertToDataUri(photo.uri);
```

### 日志

- 开发日志使用 `console.debug` 或 `console.info`。
- 可恢复问题使用 `console.warn`。
- 不可恢复问题使用 `console.error`。
- 日志包含阶段和稳定标识，不包含敏感数据。

```typescript
console.warn('[photo-processing] skipped photo', {
  photoId: photo.id,
  reason: getErrorMessage(error),
});
```

## 14. 性能与资源

- 大图不进入长期 React State；State 中保存 URI、尺寸和必要元数据。
- 不在 render 中进行 Base64 转换、图片压缩、JSON 大对象构建或排序副作用。
- FlatList 数据和回调保持稳定；大列表不得直接替换为 ScrollView。
- 临时文件、对象 URL 和请求应在不再使用时释放或取消。
- 上传前压缩分析图；导出时只处理最终入选图片。
- 对内存敏感操作设置数量、尺寸和并发上限。

## 15. 可访问性与用户错误

- 可点击元素设置正确的 `accessibilityRole`。
- 仅图标按钮必须提供 `accessibilityLabel`。
- 禁用状态同时反映到交互和 `accessibilityState`。
- 错误信息描述用户可以采取的下一步，不直接展示内部异常文本。
- 加载状态必须阻止重复提交，并允许在安全阶段取消或返回。

## 16. 提交前质量门槛

每次提交前至少运行：

```bash
npm run lint
npm run typecheck
npm run test:ci
```

涉及实际页面、相册生成或导出时，还必须执行：

```bash
npx expo start --clear
```

并在目标 iPhone 的 Expo Go 中手动检查本次改动路径。

提交不得包含：

- TypeScript 错误；
- 新增 ESLint 错误；
- 未处理的 Promise rejection；
- 明文密钥；
- 调试用 Base64、照片或大型生成文件；
- 与当前任务无关的依赖或锁文件变化。

## 17. 禁止模式

```typescript
// 禁止：跳过边界校验
const result = apiResponse as AlbumDocument;

// 禁止：吞掉错误
try {
  await exportAlbum();
} catch {}

// 禁止：无限并发处理大图
await Promise.all(photos.map(processSinglePhoto));

// 禁止：在客户端保存敏感密钥
const API_KEY = process.env.EXPO_PUBLIC_AI_API_KEY;

// 禁止：渲染期间触发副作用
function AlbumScreen() {
  void generateAlbum();
  return null;
}
```

正确做法分别是：运行时解析、明确错误处理、受控并发、后端持有密钥，以及在事件处理器或受控 Effect 中执行副作用。

## 18. 目录结构约定

```
src/
├── app/           # 路由页面 (Expo Router)
├── features/      # 功能模块
├── services/     # 服务层
├── storage/       # 存储层
├── components/    # 组件
├── hooks/         # 自定义 Hooks
├── utils/         # 工具函数
├── constants/     # 常量
└── types/         # 类型定义
```

## 19. 变更记录

涉及以下内容的变更，必须先写入 `docs/DECISIONS.md`：

- 产品逻辑变更
- 隐私相关变更
- 依赖变更
- 数据结构变更

禁止隐式引入：云服务、支付、登录、分析、真实 NFC。

## 20. 隐私与数据原则

- 所有图片、文案、数据默认本地存储
- 照片权限仅在用户点击"选择照片"时申请
- 客户端不得持有模型密钥或 API Key
- 未来云端实现必须使用服务端代理

## 21. Git 与协作流程

### 21.1 分支命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 功能 | `feature/<功能名>` | `feature/album-export` |
| 修复 | `fix/<问题>` | `fix/photo-picker-crash` |
| 重构 | `refactor/<范围>` | `refactor/state-management` |

### 21.2 Commit 格式

遵循 [Conventional Commits](https://www.conventionalcommits.org/)：

```
<type>: <描述>

[可选正文]

[可选 footer]
```

常用类型：

| Type | 用途 |
|------|------|
| `feat` | 新功能 |
| `fix` | 错误修复 |
| `docs` | 文档修改 |
| `style` | 格式调整（不影响代码） |
| `refactor` | 重构（非功能变更） |
| `test` | 测试相关 |
| `chore` | 构建/工具变更 |

示例：

```
feat: add album export to PDF
fix: handle photo picker permission denial
docs: update API response types
```

### 21.3 推送规则

- **禁止**裸 `git push --force`
- 覆盖远端只允许使用 `--force-with-lease`
- 推送前确保本地测试通过

### 21.4 协作模式（Fork 工作流）

适合开源协作或组织外贡献：

```bash
# 1. Fork 仓库后 clone 自己的 fork
git clone https://github.com/<你的用户名>/OneTapReality.git
cd OneTapReality

# 2. 添加上游仓库
git remote add upstream https://github.com/CarterWells111/OneTapReality.git

# 3. 每日开始工作时同步上游
git fetch upstream
git checkout main
git merge upstream/main

# 4. 创建功能分支
git checkout -b feature/你的功能名

# 5. 开发、提交
git add .
git commit -m "feat: 添加了什么功能"

# 6. 推送到你的 fork
git push origin feature/你的功能名

# 7. 在 GitHub 提 Pull Request
```

### 21.5 代码审查

- PR 需要至少 1-2 人 review 后再合并
- Reviewer 应检查：功能正确性、代码风格、安全性、性能影响
- 作者负责响应 review 意见并更新 PR

### 21.6 同步频率

建议每天开始工作前执行一次上游同步：

```bash
git fetch upstream && git checkout main && git merge upstream/main
```

## 22. AI 功能约束（本项目）

AI 生成功能**仅限**使用用户直接填写的内容：

**可用输入：**
- 用户填写的标题
- 用户填写的城市
- 用户填写的日期
- 照片数量

**禁止行为：**
- 读取图像像素
- 识别人物、地点、内容
- 使用 `fetch` 调用外部 API
- 使用模型 SDK
- 访问环境变量
- 请求远程 URL
- 上传照片到任何服务

**用户编辑权：**
所有 AI 生成的文本在保存前用户均可手动编辑。

---

> 上次更新：2026-07-22
